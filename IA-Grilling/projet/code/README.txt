Pour que le code fonctionne, il faut lancer l'api sur un pc en local dans l'invite de commande avec la commande: npm start ou node billets.js ensuite il faut ouvrir 
la page login.html.
Il faut aussi que les bonnes dependencies (body-parser; cors; express; helmet; morgan; node-fetch) soient installées dans le dossier node_modules: npm install nom_dependencies
